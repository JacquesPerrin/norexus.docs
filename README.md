# NOREXUS Docs

Plain Fumadocs documentation site for NOREXUS CLI, Auth, and ID.

## Development

```bash
bun install
bun run dev
```

Open `http://localhost:3000/docs`.

## Build

```bash
bun run build
```

## Content

Docs live in `content/docs`.

Add new pages to `content/docs/meta.json` so they appear in the sidebar.
